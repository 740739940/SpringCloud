server:
  port: 8881
spring:
  application:
    name: config-client
  cloud:
    config:
      label: master
      profile: dev
      uri: http://localhost:8888/
#        configclient ：启动后用bootstrap去注册中心注册，然后去appli..取配置

